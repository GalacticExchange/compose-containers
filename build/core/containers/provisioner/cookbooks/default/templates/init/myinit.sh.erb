#!/usr/bin/env bash

export TERM=xterm

#cd /opt/bootstrap && ./init.sh

# run always
echo "$(date). init" >> /opt/bootstrap/bootstrap.log

# using chef
cd /opt/bootstrap/ && chef-client -z -j /opt/bootstrap/config.json --override-runlist "recipe[server::init]"

source /usr/share/rvm/scripts/rvm
#cd /projects/ansible/provisioner && bundle
cd /projects/ansible/gush && bundle

service supervisor start
#
echo "$(date). init done" >> /opt/bootstrap/bootstrap.log
